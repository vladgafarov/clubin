import styled from 'styled-components'

const CloseButton = styled.button`
   color: white;
   font-size: 2rem;
   border: 0;
   position: absolute;
   z-index: 2;
   right: 15px;
   top: 15px;
`

export default CloseButton
